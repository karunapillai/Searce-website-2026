# Optimized build prompt — FDE page (Searce Website 2026)

Generated by `/ecc:prompt-optimizer`. Paste **Section A** into a fresh Claude Code
session to run the build. Section B is the compact variant.

All open questions are resolved (see "Decisions locked") — Section A is ready
to paste as-is.

---

## Decisions locked

All open questions are answered — this prompt is ready to paste as-is.

| Question | Decision |
|---|---|
| Animation stack | **GSAP 3 + ScrollTrigger**, version-pinned cdnjs, `fde.html` only. Accepts being the repo's first external script. |
| Dropdown | Build the **"How we solve"** dropdown (cloning the Company `.has-dropdown`/`.mega-panel` pattern) with a single item labelled **FDE** → `fde.html`. |
| File scope | `fde.html` + the pages already using the `.nav-links`/`.mega-panel` nav. `who-we-are.html` and `join-us.html` (Tailwind nav) stay untouched. |
| Palette | Match the **other preview pages** — the shared practice-page `:root` block (`--ink` / `--paper` / `--blue`). **Not** DESIGN.md's navy/daylight palette, which describes the main Next.js project. |

---

## Section A — Full prompt

```
Build a new FDE page for the Searce Website 2026 static site, and add it to a
"How we solve" navbar dropdown.

## Project context (verified — do not re-derive)

- Static HTML site. NO build step, NO package.json, NO npm. Deployed by
  .github/workflows/pages.yml from the gh-pages branch.
- Every page is one self-contained .html file at repo root, kebab-case
  (ai-adoption-change-engineering.html, business-systems-engineering.html, ...),
  with inline <style> and inline <script>. Links between pages are RELATIVE
  (href="fde.html"), never root-relative.
- The only external resources any page loads are Google Fonts stylesheets and,
  on one page, assets/preview.css. There are ZERO <script src> tags in the repo.
- Existing animation convention: IntersectionObserver + CSS @keyframes +
  requestAnimationFrame, with a @media(prefers-reduced-motion:reduce) block.
- Images/SVG/video live in assets/ at repo root.
- COLOR/TYPE SOURCE OF TRUTH: the shared practice-page :root block, which is
  byte-identical in ai-adoption-change-engineering.html,
  human-ai-work-engineering.html and ai-ready-platform-engineering.html. Copy
  it into fde.html verbatim and use ONLY these tokens:

    --ink:#000000;  --ink-85:#1a1a1c;  --ink-60:#54555a;  --ink-40:#84868c;
    --paper:#ffffff;  --paper-dim:#f6f7f9;  --paper-line:#e4e6ea;
    --blue:#0064ff;  --blue-strong:#004ecc;  --blue-deep:#00337f;
    --blue-06:#eef4ff;  --blue-12:#dbe9ff;  --blue-24:#b3d0ff;
    --display:'Poppins',system-ui,sans-serif;
    --body:'Google Sans Flex Variable','Google Sans Text','Google Sans',
           system-ui,-apple-system,sans-serif;
    --sec:88px;   /* section rhythm */
    --mod:88px;   /* modular grid cell, dark-section convention */

- Fonts load exactly as the practice pages load them: Poppins via
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap">,
  and Google Sans Flex via an inline @font-face pointing at
  https://cdn.jsdelivr.net/fontsource/fonts/google-sans-flex:vf@latest/latin-wght-normal.woff2
  (font-weight:1 1000; font-display:swap).
- DESIGN.md at repo root describes the MAIN Next.js project, not this repo. It
  is an imported reference target. Its navy/daylight palette (#001630,
  #002659, #F2F7FF) is NOT what these preview pages ship, and fde.html must
  match the preview pages, not DESIGN.md. Do not pull colors from DESIGN.md.
- Responsive breakpoints in use: max-width 1180px, 1024px, 768px, 640px.

## IMPORTANT — two corrections to the brief you may have been given

1. The "How we solve" dropdown DOES NOT EXIST yet. Across all pages,
   "How we solve" is a plain <li><a href="#">, sometimes with a decorative
   chevron SVG but no panel. The only real dropdown is "Company", which uses
   <li class="has-dropdown"> + <div class="mega-panel"> + <a class="mega-item">.
   So this task is to BUILD the How-we-solve dropdown (reusing the Company
   dropdown's markup, CSS, hover/focus behaviour and aria-haspopup verbatim),
   then add ONE item to it labelled exactly "FDE" pointing at fde.html — this is
   not "add an item to an existing dropdown", the dropdown itself is new.
   Match the surrounding nav's letter-casing per page: the *V5 pages use
   lowercase labels, the rest use Title Case. The mega-item label stays "FDE"
   (an acronym) in both.

2. The navbar is COPY-PASTED into 15 HTML files in 3 variants: lowercase labels
   (the *V5 pages), Title Case labels (most pages), and a Tailwind nav with
   root-relative /how-we-solve hrefs (who-we-are.html, join-us.html). There is
   no shared nav component.

   Scope for this task: create fde.html, and add the How-we-solve dropdown with
   the FDE entry to fde.html plus the pages that already use the
   .nav-links/.mega-panel pattern. Leave who-we-are.html and join-us.html
   (Tailwind nav, different structure) alone. If you think that boundary is
   wrong, say so before editing — do not silently widen it.

## Animation stack

DECIDED: use GSAP 3 + ScrollTrigger, loaded from a version-pinned cdnjs
<script> tag in fde.html only. This is the repo's first external script — keep
it to this page, initialize GSAP inline in fde.html, and do not refactor any
other page onto GSAP.
Wrap all scroll animation setup so it is disabled under
@media(prefers-reduced-motion:reduce) / matchMedia('(prefers-reduced-motion:
reduce)') — the existing pages all honor this, and the hero dot wave is an
infinite loop, exactly what that guard exists for.

## Figma source

Design file: https://www.figma.com/design/vOKitcGHVTS9VW37iFo9IW/Searce-website-2025?node-id=2045-780
Prototype:   https://www.figma.com/proto/vOKitcGHVTS9VW37iFo9IW/Searce-website-2025?node-id=8129-13217&starting-point-node-id=4406%3A2018&page-id=2045%3A780

STEP 0 (do this before any layout work):
- Frame NAMES are not node IDs. Call get_metadata on the page node 2045-780 to
  resolve the name->node-id map for Frame 427320989, 427320993, 427320996,
  427320998, 427321007, 427321009 and the named sections. Write that map into a
  scratch file and use it for every later call.
- Then, for EVERY frame in the section table below, pull get_design_context +
  get_screenshot + get_variable_defs. Do all of them up front so the full frame
  order and copy are confirmed before you start building.
- Download any images/SVGs the design needs via download_assets into assets/.
- Re-query the frame via MCP any time a spacing, color, or copy value is
  ambiguous. Do not guess and do not build from memory of the screenshots.
- The "tagged by Tanmay" notes are Figma COMMENTS; the Figma MCP tools cannot
  read comment threads. If a tag isn't recoverable from the design context,
  treat the 2nd->3rd fold BG transition as one continuous scroll-linked effect
  (the brief's own reading) and note the assumption in a code comment.

## Task list — build in this order, one section per step

0. Resolve the Figma node map (Step 0 above).
1. Build the How-we-solve dropdown (Company-dropdown pattern) with one
   mega-item labelled "FDE" -> fde.html; add it to every in-scope page.
   Verify keyboard access (Tab/Enter/Esc) and that the link navigates.
2. Scaffold fde.html by copying the shared practice-page chrome from
   ai-adoption-change-engineering.html: doctype, the Poppins <link> and the
   Google Sans Flex @font-face, the :root token block verbatim, the base
   resets (including the ::selection and :focus-visible rules), the nav, the
   "let's connect" band and the footer. Then add the pinned GSAP CDN tags and
   empty section shells in frame order.
3. Hero — static layout, then the dot-wave animation.
4. 2nd fold + 3rd fold BG transition.
5. Frame 427320993 — progressive-disclosure cards.
6. Frame 427320996 — right-to-left cards, inactive blurred.
7. Frame 427320998 — staggered right-to-left card entry.
8. "One initiative, one owner, no committees" — timeline + circle.
9. "Generation speed is a commodity now..." — two-phase.
10. Frame 427321007 — timeline fill.
11. Frame 427321009 — two cards entering.
12. "The same squad, sized to the moment" — Launch -> Stabilization.
13. Last section — static, no scroll animation.
14. Full-page pass: both-direction scroll, all breakpoints, console clean.

For each section: build the static layout to match the Figma frame FIRST,
verify it against the screenshot, then layer the animation on top.

## Section animation specs

| # | Frame / Section | Animation end state |
|---|---|---|
| 3 | Hero | Background dots switch on/off left-to-right in a smooth wave, looping forever, no restart glitch or visible seam. Scroll-independent. |
| 4 | 2nd fold (+3rd fold BG transition) | Scroll down: lines animate top -> middle. Scroll up: the SAME timeline reverses (one scrubbed ScrollTrigger, not a second animation). Treat 2nd->3rd fold as one continuous scroll-linked effect. |
| 5 | Frame 427320993 | Progressive disclosure: the card at the current scroll offset is sharp/highlighted, all others blurred; swaps card-by-card on scroll. |
| 6 | Frame 427320996 | Cards move right-to-left on scroll; whichever card is not active is blurred. |
| 7 | Frame 427320998 | Cards animate in right-to-left, one at a time (staggered), on scroll. |
| 8 | "One initiative, one owner, no committees" | Timeline animates left-to-right while a circle forms a semicircle right-to-left, IN SYNC, both reaching the midpoint together; then timeline finishes as the semicircle closes into a full circle. Drive both from ONE scrubbed timeline so they cannot drift. |
| 9 | "Generation speed is a commodity now..." | Phase 1: paragraph highlights progressively on scroll. Phase 2 (starts when phase 1 ends): three cards enter right-to-left in sequence, pushing the large heading left and off-screen. Card 1 = 4 lines, card 2 = 3 lines, card 3 per Figma. All three center-aligned. |
| 10 | Frame 427321007 | Timeline fills/completes as the user scrolls through the frame. |
| 11 | Frame 427321009 | Two cards enter right-to-left, one after the other, on scroll. |
| 12 | "The same squad, sized to the moment" | Right-side label transitions "Launch" -> "Stabilization", synced with the graphic morphing from rectangular dots into small square dots. |
| 13 | Last section | Static. No scroll animation. |

## Ambiguities — resolve from Figma, else assume and comment

Where Figma doesn't answer these, pick the stated default, implement it, and
leave an inline code comment marking the assumption so it's easy to revisit.

1. Hero dot-wave timing/colors -> if not in the prototype, pick values that read
   as smooth, express them as named constants at the top of the hero script, and
   comment the choice.
2. Which card is "current" in Frame 427320993 -> default to the card whose center
   is nearest the viewport center.
3. Card 3 copy/line-count in "Generation speed..." -> pull from Figma; only
   cards 1 and 2 were specified in the brief.
4. Launch -> Stabilization trigger point -> default to the section's 50% scroll
   progress unless Figma indicates otherwise.

## Technical constraints

- Visual fidelity to Figma is a strict bar, not an approximation: spacing, type
  scale, color and opacity values must match, not merely be close.
- Use ONLY the practice-page :root tokens for color. If a Figma value doesn't
  map to one, flag it rather than introducing the raw hex — and do not reach
  for a DESIGN.md token to fill the gap, since that palette belongs to the
  main project.
- Every scroll animation must be correct in BOTH directions. Prefer one
  scrubbed ScrollTrigger timeline per section over paired forward/reverse
  tweens.
- Animate transform and opacity. Avoid animating layout-affecting properties;
  for the blur effects use filter with will-change and drop it after.
- Match existing breakpoints (1180/1024/768/640) and the existing inline-CSS
  code style. No Tailwind, no CSS framework, no second animation library.
- Honor prefers-reduced-motion.

## Verification before calling any section done

- Compare the rendered section against its Figma frame screenshot.
- Scroll both directions through it; confirm it triggers AND reverses.
- Zero console errors.
- Check 1180 / 1024 / 768 / 640 px — no layout break, no horizontal scroll.
- Confirm the FDE entry in the How-we-solve dropdown navigates to fde.html from
  every page you edited.

## Do NOT

- Do not touch who-we-are.html or join-us.html (different Tailwind nav).
- Do not refactor the other pages' navs beyond adding the dropdown.
- Do not add GSAP to any page other than fde.html.
- Do not introduce new colors, a CSS framework, or a build step.
- Do not use DESIGN.md's navy/daylight palette. fde.html matches the other
  preview pages (--ink / --paper / --blue), which is a deliberate choice.
- Do not add a package.json or any tooling — this site has no build.
- Do not restructure assets/ or rename existing files.
- Do not build from screenshots or memory; pull each frame via Figma MCP.
```

---

## Section B — Quick prompt

```
Build fde.html for this static site (no build step, self-contained HTML pages,
gh-pages). Copy the :root token block, font loading, nav, connect band and
footer verbatim from ai-adoption-change-engineering.html — that shared
practice-page system is the source of truth for color and type
(--ink/--paper/--blue). Ignore DESIGN.md for color: it documents the main
Next.js project, not this repo.

Note two things the brief gets wrong: the "How we solve" dropdown doesn't exist
(only "Company" has .has-dropdown/.mega-panel — clone that pattern, then add one
mega-item labelled "FDE" -> fde.html), and the nav is copy-pasted across 15 files
in 3 variants (scope: .nav-links pages only; skip who-we-are.html and
join-us.html).

Figma: get_metadata on page 2045-780 to resolve frame names -> node IDs, then
get_design_context + get_screenshot + get_variable_defs for every frame BEFORE
building. Frames in order: 427320989 (hero), 2nd/3rd fold, 427320993, 427320996,
427320998, "One initiative...", "Generation speed...", 427321007, 427321009,
"The same squad...", last section.

GSAP 3 + ScrollTrigger from pinned cdnjs, fde.html only (this is the repo's first
external script). One scrubbed timeline per section so everything reverses.
Hero dot-wave loops independently of scroll. Guard everything behind
prefers-reduced-motion. Breakpoints 1180/1024/768/640.

Static layout per section first, verified against the Figma screenshot, then
animation. Report ambiguities as inline code comments.
```

---

## Suggested session split

This is too large for one session (11 animated sections + a nav change across
multiple files).

1. **Session 1** — Figma node map + all frame contexts pulled and saved; navbar
   dropdown built; fde.html scaffolded with tokens and empty section shells.
   End with `/ecc:save-session`.
2. **Sessions 2–4** — 3–4 sections each, static layout then animation, verified
   against Figma before moving on. Start each with `/ecc:resume-session`.
3. **Session 5** — the two timing-sensitive sections
   ("One initiative..." and "Generation speed...") on their own, since both need
   manual scroll-scrubbing to confirm sync.
4. **Session 6** — full-page pass: both-direction scroll, all four breakpoints,
   console clean, dropdown navigation from every edited page.
